INSERT INTO viaops_incident_group_correlation (
    group_rule_name,
    service_name,
    sub_service_name,
    group_start
)
VALUES (
    "${group_rule_name}",
    "${service_name}",
    "${sub_service_name}",
    "${group_start}"
)
